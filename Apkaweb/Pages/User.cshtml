@page
@model UserModel
@{
    ViewData["Title"] = "User";
}

<!DOCTYPE html>
<html>
<head>
    <title>Permissions Page</title>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <style>
        .btn-danger {
            margin-right: 5px;
            margin-top: 10px;
            padding: 10px;
            font-size: 20px;
            width: 100px;
            align-content: center;
            margin-left: 5px;
        }

        .box {
            margin: 5px;
            margin-top: 15px;
            padding: 10px;
            border-radius: 5px;
            border: 1px solid #ccc;
            background-color: #f9f9f9;
            max-width: 100%;
            text-align: left;
        }

        .flex-container {
            display: flex;
            align-items: center;
        }

        table {
            width: 100%;
            border-collapse: collapse;
        }

        th, td {
            padding: 8px;
            text-align: left;
        }

        tr:not(:last-child) td,
        tr:not(:last-child) th {
            border-bottom: 1px solid #ddd;
        }

        .switch {
            position: relative;
            display: inline-block;
            width: 60px;
            height: 34px;
        }

            .switch input {
                opacity: 0;
                width: 0;
                height: 0;
            }

        .slider {
            position: absolute;
            cursor: pointer;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background-color: #ccc;
            -webkit-transition: .4s;
            transition: .4s;
            border-radius: 34px;
        }

            .slider:before {
                position: absolute;
                content: "";
                height: 26px;
                width: 26px;
                left: 4px;
                bottom: 4px;
                background-color: white;
                -webkit-transition: .4s;
                transition: .4s;
                border-radius: 50%;
            }

        input:checked + .slider {
            background-color: #2196F3;
        }

        input:focus + .slider {
            box-shadow: 0 0 1px #2196F3;
        }

        input:checked + .slider:before {
            -webkit-transform: translateX(26px);
            -ms-transform: translateX(26px);
            transform: translateX(26px);
        }

        #toggle-on {
            display: none;
        }
    </style>
</head>
<body>
    <div class="d-flex justify-content-between">
        <h1>My account</h1>
        <a href="/options" class="btn btn-danger"><b>Go back</b></a>
    </div>
    <div class="box">
        <table>
            <tr>
                <th>Username</th>
                <td>nazwa użytkownika</td>
            </tr>
            <tr>
                <th>Password</th>
                <td>hasło</td>
            </tr>
            <tr>
                <th>The date of the last unsuccessful login attempt</th>
                <td>data</td>
            </tr>
            <tr>
                <th>The date of the last successful login attempt</th>
                <td>data</td>
            </tr>
            <tr>
                <th>The number of unsuccessful login attempts since the last successful login</th>
                <td>data</td>
            </tr>
        </table>
    </div>
    <div class="box">
        <div class="flex-container">
            <a>Do you want to enable the account lock?</a>
            <label class="switch" style="margin-left: 20px;">
                <input type="checkbox" id="lockSwitch">
                <span class="slider"></span>
            </label>
        </div>
        <div class="flex-container" id="toggle-on">
            <a>Choose a number of attempts before the account is locked</a>
            <select id="mySelect" style="margin-left: 15px;">
                <option value="">choose an option</option>
                <option value="1">1</option>
                <option value="2">2</option>
                <option value="3">3</option>
                <option value="4">4</option>
                <option value="5">5</option>
            </select>
            <button class="btn btn-outline-primary" style="margin-left: 15px;">Save</button>
        </div>
    </div>
</body>
</html>

<script>
    var lockSwitch = document.getElementById('lockSwitch');
    var toggleDiv = document.getElementById('toggle-on');

    lockSwitch.addEventListener('change', function () {
        toggleDiv.style.display = this.checked ? 'block' : 'none';
    });
</script>



<!--<h1>User Page</h1>

<div>
    <p>Username: @Model.Username</p>
    <p>Password: @Model.Password</p>
    <p>Failed Login Attempts: @Model.FailedLoginAttempts</p>
        @if (Model.IsBlockEnabled == 1)
        {
            <p>Is Blocked: true</p>
        }
        else
        {
            <p>Is Blocked: false</p>
        }

    <p>Number of Attempts: @Model.NumberOfAttempts</p>
    <p>Failed Attempt Date: @Model.FailedAttemptDate.ToString("yyyy-MM-dd HH:mm:ss")</p>
    <p>Success Attempt Date: @Model.SuccesAttemptDate.ToString("yyyy-MM-dd HH:mm:ss")</p>
</div>

<form method="post">
    <input type="hidden" name="userId" value="@Model.UserId" />
    <button type="submit" class="btn btn-primary" asp-page-handler="ToggleBlock">
        @if (Model.IsBlockEnabled == 1)
        {
            <span>Wyłącz blokowanie konta</span>
        }
        else
        {
            <span>Włącz blokowanie konta</span>
        }
    </button>
</form>

@if (Model.IsBlockEnabled == 1)
{
    <form method="post">
        <input type="hidden" name="userId" value="@Model.UserId" />
        <div class="form-group">
            <label for="numberOfAttempts">Set Number of Attempts:</label>
            <input type="number" class="form-control" id="numberOfAttempts" name="numberOfAttempts" value="3" min="1" />
        </div>
        <button type="submit" class="btn btn-primary" asp-page-handler="SetNumberOfAttempts">Set Number of Attempts</button>
    </form>
}

<a class="btn btn-primary" asp-page="/ChangePassword">Change Password</a>
-->
